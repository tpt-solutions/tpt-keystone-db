apiVersion: apps/v1
kind: Deployment
metadata:
  name: tpt-operator
  namespace: tpt-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: tpt-operator
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tpt-operator
    spec:
      serviceAccountName: tpt-operator
      containers:
        - name: tpt-operator
          image: ghcr.io/example/tpt-operator:0.1.0 # replace with your built/pushed image
          env:
            - name: RUST_LOG
              value: info
